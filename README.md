# README

## 项目结构

```sh
(root)
├── vars/
│   └── hello.groovy        ← 简单函数放这里，名字就是函数名
├── src/                    ← 复杂类或工具代码（可选）
│   └── org/example/MyUtil.groovy
├── resources/              ← 模板文件等资源（可选）
└── README.md
```